{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API using json-server for Anki project",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ton Nom",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
